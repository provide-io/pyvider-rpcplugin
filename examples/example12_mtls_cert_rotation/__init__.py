# This file makes Python treat the directory examples/example12_mtls_cert_rotation as a package.

# You can optionally import key components here for easier access if needed,
# but for this example, it's primarily to ensure relative imports in
# server.py and client.py (e.g., from . import service_pb2) work correctly
# when scripts are run from the project root or as part of a larger system.

# For example, if you wanted to run these programmatically:
# from .client import main as run_client
# from .server import main as run_server

# For now, it can remain empty.
