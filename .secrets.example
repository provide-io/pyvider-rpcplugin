# Example secrets file for act testing
# Copy this file to .secrets and add your actual tokens if needed

# For codecov integration (optional)
CODECOV_TOKEN=test-codecov-token

# For PyPI publishing (required for release workflow)
PYPI_API_TOKEN=pypi-test-token
TEST_PYPI_API_TOKEN=test-pypi-test-token

# GitHub token (usually not needed, act provides one)
GITHUB_TOKEN=test-github-token