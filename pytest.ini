[pytest]
testpaths = tests
pythonpath = src
addopts = -v --tb=short
markers =
    slow: marks tests as slow running
filterwarnings =
    ignore::pytest.PytestCollectionWarning:*TestRequest*
    ignore::pytest.PytestCollectionWarning:*TestResponse*
    ignore::pytest.PytestCollectionWarning:*TestStub*